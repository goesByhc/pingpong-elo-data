# SQLite runtime files
*.db
*.db-shm
*.db-wal
*.log

# Runtime backups & logs
backup_clean/
_attic/
logs/

# ITTF login cookies (secrets - never commit)
ittf_cookies.json

# Regenerable intermediates
matches_formatted/
matches/*_names_warn.log
sources_manifest.json

# format_matches 输出目录（已弃用，不再入库）
formatted/

# 开发中间产物（一次性抓取/补全缓存，合并进 players.csv 后即无用，不入 git）
_*.json
sources/cttsl_2026_raw/
